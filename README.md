# avsc-schema-to-json-wrapper

A scala wrapper for fge/json-schema-avro

Convert avro schema (.avsc) to json schema

Run AvscSchemaToJson with 2 parameters: input (containing avsc files) and output directories
